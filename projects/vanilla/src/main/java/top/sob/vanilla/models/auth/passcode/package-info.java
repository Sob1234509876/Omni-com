/**
 * A specific handler, or specific authorizer. A user register or log into the server and adds it`s inet address into
 * the server`s login cache.
 */
package top.sob.vanilla.models.auth.passcode;