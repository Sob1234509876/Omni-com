plugins {
    id 'top.sob.idp'
}

group = 'top.sob.api'
version = '1.2.8a SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation libs.junit.jupiter

    implementation libs.api, libs.jannotations, libs.log4J
}

test {
    useJUnitPlatform()
}