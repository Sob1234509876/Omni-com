/**
 * A specific handler, or Specific Language Translator. Does actually nothing, you give a string, and it returns the same
 * string with no changes (not even in memory address).
 */
package top.sob.core.models.translation.enUs;