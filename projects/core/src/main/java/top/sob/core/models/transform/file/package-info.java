/**
 * A specific handler, or a specific protocol transformer. This is a file protocol transformer, it extends URL classpath
 * that have a file protocol, and replace them with the files in the folder URLs (URLs that represent a folder in your
 * machine).
 */
package top.sob.core.models.transform.file;